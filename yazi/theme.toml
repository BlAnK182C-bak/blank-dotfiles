# Auto-generated — do not edit

# ── Manager ────────────────────────────────────────────────────────────────
[manager]
cwd = { fg = "#dcc9c5", bold = true }

hovered = { fg = "#0d0d0d", bg = "#dcc9c5", bold = true }
preview_hovered = { underline = true }

find_keyword  = { fg = "#dcc9c5", bold = true, underline = true }
find_position = { fg = "#845147", bold = true }

marker_selected = { fg = "#0d0d0d", bg = "#845147", bold = true }

tab_active   = { fg = "#0d0d0d", bg = "#dcc9c5", bold = true }
tab_inactive = { fg = "#845147", bg = "#1e1e1e" }

border_symbol = "│"
border_style  = { fg = "#845147" }

# ── Status ─────────────────────────────────────────────────────────────────
[status]
separator_open  = ""
separator_close = ""

mode_normal = { fg = "#0d0d0d", bg = "#dcc9c5", bold = true }
mode_select = { fg = "#0d0d0d", bg = "#845147", bold = true }
mode_unset  = { fg = "#0d0d0d", bg = "#1e1e1e", bold = true }

progress_label  = { bold = true }
progress_normal = { fg = "#dcc9c5", bg = "#1e1e1e" }
progress_error  = { fg = "#cc6666", bg = "#1e1e1e" }

# ── Which (key hints) ──────────────────────────────────────────────────────
[which]
mask      = { bg = "#1e1e1e" }
cand      = { fg = "#dcc9c5", bold = true }
desc      = { fg = "#845147" }
separator = "  "

# ── Input ──────────────────────────────────────────────────────────────────
[input]
border   = { fg = "#dcc9c5" }
title    = { fg = "#dcc9c5", bold = true }
selected = { reversed = true }

# ── Completion ─────────────────────────────────────────────────────────────
[completion]
border   = { fg = "#845147" }
active   = { fg = "#0d0d0d", bg = "#dcc9c5", bold = true }
inactive = { fg = "#845147" }

# ── Tasks ──────────────────────────────────────────────────────────────────
[tasks]
border  = { fg = "#dcc9c5" }
title   = { fg = "#dcc9c5" }
hovered = { underline = true }

# ── Filetype colors ────────────────────────────────────────────────────────
[filetype]
rules = [
    { mime = "image/*", fg = "#845147" },
    { mime = "video/*", fg = "#dcc9c5" },
    { mime = "audio/*", fg = "#dcc9c5" },
    { mime = "application/*", fg = "#dcc9c5" },
    { mime = "text/*",  fg = "#dcc9c5" },
]
